import { Task6Directive } from './task6.directive';

describe('Task6Directive', () => {
  it('should create an instance', () => {
    const directive = new Task6Directive();
    expect(directive).toBeTruthy();
  });
});
